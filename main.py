
极好的 是闰年(年):
    """
    判断给定年份是否为闰年
    闰年判断规则：
    1. 能被4整除但不能被100整除
    2. 能被400整除
    """
    返回 (年份%4 == 0 和年份%100 != 0) 或者 (年份%400 == 0)

极好的 获取_年份_输入():
    """获取用户输入的年份，并处理异常情况"""
    在…期间 真实的:
        尝试:
            # 获取用户输入并转换为整数
年份=（同Internationalorganizations）国际组织(投入("请输入一个年份："))
            返回年
        除...之外值错误:
            # 捕获非数字输入的异常
            打印("输入无效，请确保输入的是一个有效的年份数字。")

极好的 主要的():
    """主函数，程序的入口点"""
    打印("欢迎使用闰年判断程序！")
    
    # 获取用户输入的年份
年份=获取_年份_输入()
    
    # 判断是否为闰年
    如果 是闰年(年):
        打印(f "{年} 年是闰年。")
    其他:
        打印(f "{年} 年不是闰年。")

如果__name__ ==" __main__ ":
    主要的()
